<Window x:Class="Grade_project.Windows.AddEditSpecialityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить/Редактировать специальность" 
        Height="300" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Название специальности -->
        <TextBlock Text="Название специальности:" Grid.Row="0" />
        <TextBox x:Name="SpecNameTextBox" Grid.Row="0" Grid.Column="1" Text="{Binding Path=DataContext.Speciality.SpecName, ElementName=RootGrid}" />


        <!-- Дата расформирования -->
        <TextBlock Text="Дата расформирования:" Grid.Row="2" />
        <DatePicker x:Name="DateDisbandmentTextBox"
                    SelectedDate="{Binding Path=DataContext.Speciality.DateDisbandment, ElementName=RootGrid}"
                    Grid.Row="2" Grid.Column="1" />

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Click="SaveButton_Click" Width="100" Margin="0,0,5,0"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>